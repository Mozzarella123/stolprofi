'use strict';

import Map from '../map';

describe('Map View', function() {

  beforeEach(() => {
    this.map = new Map();
  });

  it('Should run a few assertions', () => {
    expect(this.map);
  });

});
