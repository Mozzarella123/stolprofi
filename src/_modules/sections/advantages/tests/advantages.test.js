'use strict';

import Advantages from '../advantages';

describe('Advantages View', function() {

  beforeEach(() => {
    this.advantages = new Advantages();
  });

  it('Should run a few assertions', () => {
    expect(this.advantages);
  });

});
